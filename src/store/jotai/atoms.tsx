import { atom } from "jotai";

export const langAtom = atom(0);
export const darkModeAtom = atom(false);

export const viewportTopAtom = atom(0);
export const pageOffsetTopAtom = atom(0);

export const isOpenDrawerHumMenuAtom = atom(false);
